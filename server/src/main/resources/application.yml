server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ota?useSSL=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: fuxing1234
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis-plus:
  mapper-locations: classpath:mapper/*.xml
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

ota:
  storage:
    type: local
    local-path: ./data/apk
