<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ota"
    android:sharedUserId="android.uid.system">

    <!-- Network -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Boot -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Foreground Service (API 33) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <!-- Silent Install (system app) -->
    <uses-permission android:name="android.permission.INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.DELETE_PACKAGES" />

    <!-- Request install (fallback) -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <application
        android:name=".OtaApplication"
        android:allowBackup="false"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/Theme.OTA"
        android:usesCleartextTraffic="false">

        <!-- Main Process Activity -->
        <activity
            android:name=".ui.UpdateActivity"
            android:exported="true"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Boot Receiver (main process) -->
        <receiver
            android:name=".receiver.BootReceiver"
            android:directBootAware="true"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- Install Result Receiver (runs in :ota process) -->
        <receiver
            android:name=".install.InstallResultReceiver"
            android:exported="false"
            android:process=":ota" />

        <!-- Daemon Service (runs in :ota process) -->
        <service
            android:name=".service.UpdateService"
            android:exported="false"
            android:foregroundServiceType="dataSync"
            android:process=":ota">
            <intent-filter>
                <action android:name="com.example.ota.IUpdateService" />
            </intent-filter>
        </service>

    </application>

</manifest>
